# URL do relay de tempo real (colaboração 3D + CRM).
# Produção (Vercel): o relay hospedado no Render.
VITE_COLLAB_WS_URL=wss://linear-realtime-relay.onrender.com
# API publica do CRM onde /suporte-3d le os leads do Estudio 3D.
VITE_CRM_API_BASE_URL=https://crm-marcenaria.vercel.app/api
# Dev local (relay rodando no seu PC via `npm run server`): ws://localhost:8787
